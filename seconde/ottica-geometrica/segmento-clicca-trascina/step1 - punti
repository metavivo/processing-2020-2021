int x1, x2, y1, y2;
 
void setup() { 
    size(700,400);
    background(255);    
    fill(0);    
} 
 
void draw() { 
}
 
 
void mousePressed() {
    ellipse (mouseX, mouseY, 5, 5);
}
 
 
void mouseReleased() {
    ellipse (mouseX, mouseY, 4, 4);
}
